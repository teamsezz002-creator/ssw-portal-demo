import React, { useState, useEffect } from 'react';
import { motion } from 'motion/react';
import { Gamepad2, Star, Clock, ChevronRight, Loader2, Library as LibraryIcon } from 'lucide-react';
import { Link } from 'react-router-dom';
import { useSimulationsSync } from '../data';
import { Simulation } from '../types'; export function Home() { const [simulations, setSimulations] = useState<Simulation[]>([]); const [loading, setLoading] = useState(true); useEffect(() => { return useSimulationsSync((newData) => { setSimulations(newData); setLoading(false); }); }, []); if (loading) { return ( <div className="flex items-center justify-center min-h-[50vh]"> <Loader2 className="w-10 h-10 animate-spin text-blue-500" /> </div> ); } if (simulations.length === 0) { return ( <div className="flex flex-col items-center justify-center min-h-[50vh] text-slate-500 dark:text-slate-400 gap-4"> <LibraryIcon className="w-16 h-16 opacity-50" /> <h2 className="text-xl font-bold">No simulations found</h2> <p>Head to the Studio to create your first simulation!</p> <Link to="/studio" className="mt-4 px-6 py-2 bg-blue-600 hover:bg-blue-700 text-white rounded-full font-bold transition-all">Go to Studio</Link> </div> ); } const featuredSim = simulations.find(s => s.featured) || simulations[0]; const newReleases = simulations.slice(0, 4); const trending = simulations.slice(2, 6); const continueLearning = simulations.slice(1, 3); return ( <div className="flex flex-col gap-12 pb-10"> {/* Hero Section */} <motion.section initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.5, ease: "easeOut" }} className="relative w-full h-[300px] sm:h-[400px] lg:h-[500px] rounded-[2rem] overflow-hidden group border border-slate-200 dark:border-white/5 " > <video src="/video.mp4" className="absolute inset-0 w-full h-full object-cover" autoPlay muted loop playsInline /> <div className="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent flex items-end p-6 sm:p-10"> <div className="max-w-2xl text-white"> <span className="px-3 py-1 bg-blue-600 rounded-full text-xs font-bold mb-3 inline-block">Featured</span> <h1 className="text-3xl sm:text-5xl font-bold mb-2">{featuredSim.title}</h1> <p className="opacity-80 text-sm sm:text-base line-clamp-2 md:line-clamp-none">{featuredSim.description}</p> <Link to={`/play/${featuredSim.id}`} className="mt-6 inline-flex bg-white text-black px-6 py-3 rounded-xl font-bold items-center gap-2 hover:bg-blue-50 transition-colors"> <Gamepad2 className="w-5 h-5 fill-black" /> Play Now </Link> </div> </div> </motion.section> {/* Rows */} {continueLearning.length > 0 && <SimRow title="Continue Learning" simulations={continueLearning} isContinue />} {trending.length > 0 && <SimRow title="Trending Right Now" simulations={trending} />} {newReleases.length > 0 && <SimRow title="New Releases" simulations={newReleases} />} </div> );
} function SimRow({ title, simulations, isContinue }: { title: string; simulations: Simulation[]; isContinue?: boolean }) { if (isContinue) { return ( <section className="flex flex-col gap-4"> <div className="flex items-center justify-between mb-2"> <h2 className="text-xl font-bold tracking-tight text-slate-900 dark:text-slate-100">{title}</h2> </div> <div className="flex flex-col lg:flex-row space-y-4 lg:space-y-0 lg:space-x-6"> {simulations.map((sim, index) => { const isPhysics = sim.category === 'Physics'; return ( <motion.div key={sim.id} initial={{ opacity: 0, y: 10 }} animate={{ opacity: 1, y: 0 }} transition={{ duration: 0.5, delay: index * 0.1 }} className="flex-1" > <Link to={`/play/${sim.id}`} className="w-full glass-panel p-4 rounded-2xl flex items-center space-x-4 transition-all duration-300 shadow hover: border border-slate-200/50 dark:border-white/5" > <div className={`w-16 h-16 ${isPhysics ? 'bg-blue-100 dark:bg-blue-600/20' : 'bg-purple-100 dark:bg-purple-600/20'} rounded-xl flex items-center justify-center shrink-0 overflow-hidden relative`}> <img src={sim.thumbnail} alt={sim.title} className="absolute inset-0 w-full h-full object-cover" /> </div> <div className="flex-1 min-w-0"> <p className="text-xs text-slate-500 font-medium truncate">{sim.category} • {sim.targetClass}</p> <p className="font-bold text-slate-900 dark:text-slate-100 truncate">{sim.title}</p> </div> </Link> </motion.div> ) })} </div> </section> ); } return ( <section className="flex flex-col gap-4"> <div className="flex items-center justify-between mb-2"> <h2 className="text-xl font-bold tracking-tight text-slate-900 dark:text-slate-100 flex items-center"> <span className="w-1.5 h-6 bg-blue-500 rounded-full mr-3"></span> {title} </h2> <span className="text-xs text-blue-600 dark:text-blue-400 font-bold cursor-pointer hover:text-blue-500 dark:hover:text-blue-300 transition-colors">Explore all</span> </div> <div className="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-4 gap-4 sm:gap-6"> {simulations.map((sim, index) => { let categoryColor = 'text-blue-600 dark:text-blue-400'; if (sim.category === 'Biology') categoryColor = 'text-emerald-600 dark:text-green-400'; if (sim.category === 'Chemistry') categoryColor = 'text-purple-600 dark:text-purple-400'; if (sim.category === 'Math') categoryColor = 'text-amber-600 dark:text-orange-400'; return ( <motion.div key={sim.id} initial={{ opacity: 0, scale: 0.95 }} animate={{ opacity: 1, scale: 1 }} transition={{ duration: 0.5, delay: index * 0.1 }} > <Link to={`/play/${sim.id}`} className="aspect-square bg-white dark:bg-[#111116] rounded-[2rem] border border-slate-200 dark:border-white/5 overflow-hidden relative group cursor-pointer block shadow transition-all duration-300 hover:" > <img src={sim.thumbnail} alt={sim.title} className="absolute inset-0 w-full h-full object-cover transition-transform duration-500 group-hover:scale-105" /> <div className="absolute inset-0 bg-gradient-to-t from-black/80 via-black/20 to-transparent"></div> <div className="absolute bottom-3 left-4 right-4 text-white"> <p className={`text-[10px] font-bold mb-0.5 ${categoryColor}`}>{sim.category}</p> <p className="text-sm font-bold truncate w-full">{sim.title}</p> </div> <div className="absolute inset-0 flex items-center justify-center opacity-0 group-hover:opacity-100 transition-opacity bg-blue-600/20 backdrop-blur-[2px]"> <div className="w-12 h-12 bg-white rounded-full flex items-center justify-center text-black shadow-black/20 scale-90 group-hover:scale-100 transition-transform"> <Gamepad2 className="w-6 h-6 fill-black" /> </div> </div> </Link> </motion.div> )})} </div> </section> );
}
